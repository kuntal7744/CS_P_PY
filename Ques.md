1. Basic Input/Output:
   - Take your name and age as input and Print Message : "Hello, <Name>! You are <Age> Years Old."
   - Swap Two Numbers without using a Third variable.
   - Find the area of a Rectangle and Circle
   - Check if a Nuber is Even or Odd.

2. Conditional Statement:
   - Check if a Number is Positive, Negative, or Zero.
   - Find the Largest of 3 Numbers.
   - Check if its a Year is a Leap Year or Not (400/100/4)
   - Write a Simple Calculator using if.

3. Loops:
   - print Numbers 1 to 10 using both for and while loops.
   - Print Multiplication table of any number given by user.
   - Print the Sum of all Even numbers between 1 to 100.
   - Print Factorial of a Given Number.
   - Print Fibonacci Series up to (n) terms
   - Count didgits, Letters, and spaces in a Given string.
   - Reverse Numbers.

4. Lists, Tuples, sets, Directories:
   - tale 5 Numbers as input and store them in List, Then Print the Sum and Average.
   - Find the Largest and Smallest Number in a List without using max() or min().
   - remove Duplicates from a List using a set.
   - Create a Dictionary with stu_names as keys and make as Values:
             - print the name of the studs with Higest Marks.
   - Write a Program to Count the Frequency of each word in a sentence(Dict_usage)

5. Functions:
   - Write a Function to check if a number is prime.
   - Write a Function that returns the Factorial of a Number
   - write a Function that takes a list of numbers and returns the sum of even numbers.
   - write a function that takes string and returns it reveresed.
   - write a function that checks if a string is a palindrome or not.
  
6. Exception Handling:
   - write a program that divides two numbers and handles (ZeroDivisionError)
   - Handle (ValueError) when user inputs non-numeric data.
   - create a program that opens a file and Handles (FileNotFoundError)
   - ask user for a Number, Handle any excepttion if input is invalid and keep asking until correct input is given
   - Combine Multiple Exceptions in one Program.
  
====================================================================================================================================================================================================================
====================================================================================================================================================================================================================
* Real_Time_Questions:

  1. Write Nested Loop - where the first Loop's Range Should be (10) and Inner Loops range should be (4)
  2. Woring with lists: -> List all the Files in the List of folders that user Provides.
  3. Exception_handling:
     - List all the Files in the List of Folders that user Provides.
     - Write a Program for (ZeroDivisionError)/(ValueError).
  4. Wrking with Dictionaries and Sets:
     - Dictionary
     - Nested Dictionaries
     - Sets
     - Get the Pull Request Information on "GitHub Repo"
  5. File Operations:
     - Update (server.conf) file - when "MAX_CONNECTIONS" on the server Reach=600, write a python Program file to update the "Server.conf" file and update the key called "max_connections" from 600 to 1000.
  
